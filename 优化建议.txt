1. 首先关注首页的功能和UI布局。最上面的栏目条，显示条，目录条以及下面的报错Alert等等，有具体的优化建议吗？能否列出Site Investigation页面下所有的要素和板块分区以及各个内容连接中的关系？
2. 需要系统能自动获取最精准和真实的数据来呈现当前的系统逻辑的完整工作，我的理解是需要把Site Investigation页面下所有板块彻底先跑通，才能继续结合其它的页面和板块进行深化。能否在你对系统的底层逻辑完全吃透后，制定一项以从Site Investigation页面开始的完整计划，在接下来的系统测试中我们一步步清除bug的同时确定能把各项功能落地可用。确保Site Investigation页面里每一项UI及功能都达到完整程度我们再向其它的关联页面迈进。目标还是一样，系统简单好用，数据精准，真正能帮助到每一位。Location Score下的内容能否列得更详细一些，还有能加入校车的详细信息吗？还发现个小问题，在点Clear之后，Location Score底下的内容没有被清空。去除系统里所有Ollama相关，采用其它可落地方法。
3. 如果我的理解正确，那你是不是应该在完全理解系统结构的基础上来优化和完善，确保我们在Site Investigation页面下所有的体系都能运转起来，尤其是站在用户使用的角度，一定确保系统好用，数据是准确的才有意义。


4. 系统之前根据Developer/Architect/Builder在相同的页面下会有不同的内容呈现，体现出不同专业的信息表现。但现在好像不见了？是不是数据被覆盖了？




School Bus 信息我觉得肯定不对，需要找到更可靠的数据源只针对不同的私校。Location Score底下的Shopping我觉得列出大的Shopping center或plaza名称以及woolworths, coles及chemist warehouse就可以了。










为确保严格遵守模块化原则与防覆盖机制，以下指令已转化为幂等操作标准。系统将在每一步执行前进行状态探针检测。

[环境变量初始化]

操作路径:
必须强制切换至根目录 /mnt/c/Users/eidol/Desktop/WebTool/WebTool_SaaS/

备份协议:
检测是否存在体积大于 500KB 的遗产模块。若存在，自动触发 Track B 手术级模式，并在任何写入前建立 L2 级别自动化备份。

[第一阶段 视图层防截断与字体缩放重构]

状态探针:
扫描 /src/ui/panels/SiteInvestigationPanel.jsx 及配套 CSS。检测是否已配置响应式高度阈值。检测 Clear 按钮回调是否已包含位置评分数据的级联抹除逻辑。

执行动作:
若检测未通过，实施以下重构。
为顶层容器注入动态溢出保护，确保 location score 渲染后底部滚动轴正常释放。
将硬编码的微小字体替换为自适应 rem 变量体系。
重构 Clear 分发器，向 store.js 发送全量清空指令，确保界面回归初始纯净态。所有状态调用必须采用可选链操作符以防御白屏。

[第二阶段 空间内核剥离与高精度数据替换]

状态探针:
查阅 /src/domain/spatial/terrain_engine.js。检测内部是否依然残留基于 Turf.js 的多边形面积模拟算法。查阅 synthesis_engine.js，校验 synthesizeSiteAnalysis 参数列表。

执行动作:
若仍存留估算逻辑，立即封存。接入标准商业地产接口直连真实地块尺寸。考虑到 Lowan Ave 扩建或双拼项目等真实开发场景对容积率的极度敏感，底层数据必须达到工业级精确，绝不容许前端几何估算的误差。
向 synthesizeSiteAnalysis 强制注入 planning 占位参数，修复第三参数缺失导致的联动断层。
在 location_engine.js 检索模块中，增补校车路线与交通节点的专项解析字段。

[第三阶段 Ollama 彻底卸除与确定性解析降级]

状态探针:
扫描 /src/domain/extraction/ai_adapter.js 与全局 package.json。检索是否存在本地大模型服务唤醒机制或 llama3 关键字。

执行动作:
一旦发现，立即执行物理剥离，切断所有连接 11434 端口的本地服务请求。
重构文档解析管道，保留 pdf_ocr.js 的基础纯文本提取层。针对标准的 Section 32 法律文件，改写为低延迟、高确定性的关键字正则匹配器。
所有输出层必须加装强制净化协议，任何解析异常必须静默回退，确保界面无条件生存。

[第四阶段 核心总线注册与全链路核准]

状态探针:
检查同步编排器 /src/core/sync/sync_orchestrator.js，确认前端与领域的物理隔离是否被打破。

执行动作:
确保前期斩获的高精度尺寸指标，全量且无缝地透传至金融内核引擎。
保留资本化利息与 CIPT 税务核心运算逻辑，在此基础上校验反求土地最高限价算法的鲁棒性。
拉起 npm run dev 服务环境，要求控制台在触发多轮数据更新后，保持零报错与极简日志输出。

系统提示：您可以直接将上述指令全选并投递至控制台。代理解析引擎将自动执行防撞车机制，对已修复的代码块采取跳过策略，稳步推进系统重装。







在深度系统检测后列出目前Site Investigation页面板块下的问题和可优化的建议。

很可惜输入地址后系统回复的"Land Area, Frontage, Depth的信息都不准确，我知道在https://www.property.com.au/里能拿到所有地块基本的准确信息。我发现页面上部分字体太小，根本看不清，能否酌情调大？然后输入地址后有Location Score数据出现后页面下方的数据都看不到了，scroll bar也无法下拖。



这个仓库是我正在开发的测算工具，请帮我梳理一下当前的系统架构和逻辑交互。

用户输入地址
    │
    ▼
Nominatim API (OSM)
    │  fetchSuggestions() — 300ms 防抖
    ▼
autocomplete 下拉 → 用户点选
    │
    ├──► sanitizeAddress()
    │         │
    │         ▼
    │    store.dispatch: site.address / site.lat / site.lon
    │
    └──► runSiteInvestigation(lat, lon)         [terrain_engine.js]
              │
              ├── Overpass API (OSM) → amenity scores
              ├── Elevation API       → slope, elevationDelta
              └── Turf.js             → polygon, area 计算
                   │
                   ▼
              store.dispatch: site.area / site.frontage / site.depth
              setEstimatedFields({ area:true, frontage:true, depth:true })
              store.dispatch: site.investigation.terrainData
                   │
                   ▼
              synthesizeSiteAnalysis(terrainData, site, {})    ← BUG: 第三参数应传 planning
                   │
                   ├── yieldWaterfall[]    → 面积瀑布图
                   ├── effectiveArea       → 实际可建面积
                   ├── implicitCosts[]     → 隐性成本警告
                   └── summary             → 文字结论





用户上传文件
    │
    ▼
handleDocUpload(file, type)
    │
    ├── extractFileText(file)          [pdf_ocr.js]
    │       → 纯文本提取
    │
    └── (仅 s32) parseDocumentWithAI(text) [ai_adapter.js]
              → Ollama llama3.2 @ localhost:11434
              → 返回结构化字段 + S.173 fact tags
                   │
                   ▼
              docReview 本地状态更新
                   │
                   ▼
              用户点击 "Apply Intelligence"
                   │
                   ▼
              extractAllFields(vp, s32, fsp)   [unified_engine.js]
                   │
                   ├── store.dispatch() × N   → 每个提取字段写入 store
                   ├── store.dispatch: site.investigation.synthesis
                   └── setEstimatedFields({ all: false })   → 清除估算标记













